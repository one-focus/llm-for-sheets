# clasp pushes every .js and .html it finds. Only the four add-on files belong
# in the script project — selfcheck.js is a node script whose top-level
# require() would throw on every execution, and docs/ is the website.
selfcheck.js
docs/**
google*.html
*.md
LICENSE
.gitignore
.claspignore
node_modules/**
.git/**
